import { MenuHover } from './menu-hover';

describe('MenuHover', () => {
  it('should create an instance', () => {
    const directive = new MenuHover();
    expect(directive).toBeTruthy();
  });
});
